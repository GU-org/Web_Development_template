Assignment 2: Dynamic Product Filter (jQuery)
🎯 Objective

Practice:

.filter() (jQuery DOM filtering, not array)
.show() / .hide()
Working with classes

🧩 Requirements

Create a product list and filter them by category.

🧑‍💻 Your Tasks
🔹 Task 1: Handle Button Click
// TODO: when a filter button is clicked
// get category from data attribute

🔹 Task 2: Filter Items
// if "all" → show all items

// otherwise:
// - hide all
// - show only items with matching class

🔹 Task 3: Use jQuery Methods

Use:

.click()
.hide()
.show()
.filter()
🧪 Example Behavior
Click "Fruits" → only fruits visible
Click "Vegetables" → only vegetables
Click "All" → show everything

⭐ Bonus
Add animation using .fadeIn() / .fadeOut()
Highlight active button